Quando atualizar o código siga as etapas:

git add .
git commit -m "mensagem"
git push


Na máquina virtual na nuvem vps server:
entra com o ip:
ssl root@ip
digita a senha
navega para a pasta
/var/www/fastfood-bot
digita os comandos git para atualizar
git pull

depois reinicia os serviços
pm2 restart all
pm2 list

